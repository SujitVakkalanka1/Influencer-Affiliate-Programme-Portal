import { Filter, Search } from "lucide-react";
import DashboardShell from "../components/layout/DashboardShell.jsx";
import Button from "../components/ui/Button.jsx";
import Card from "../components/ui/Card.jsx";
import Table from "../components/ui/Table.jsx";
import { adminUsers, campaigns, payouts } from "../data/dashboardData.js";
export default function AdminDashboard() {
  return <DashboardShell admin title="Admin Dashboard" subtitle="Manage creators, payouts, and campaign health."><section className="mb-8 grid gap-4 md:grid-cols-3">{[["Active Creators","2,840","+12%"],["Payout Queue","$18,420","9 pending"],["Campaign ROI","4.2x","Strong"]].map(([label,value,meta]) => <Card key={label}><p className="text-sm text-white/45">{label}</p><p className="mt-4 text-3xl font-black">{value}</p><p className="mt-2 text-sm font-semibold text-ember-500">{meta}</p></Card>)}</section><div className="mb-6 flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div className="flex max-w-md flex-1 items-center gap-3 rounded-full border border-white/[0.08] bg-white/[0.03] px-4 py-3 text-white/45"><Search className="h-4 w-4" /><input className="w-full bg-transparent text-sm outline-none placeholder:text-white/35" placeholder="Search influencers" /></div><Button variant="secondary"><Filter className="h-4 w-4" />Filter</Button></div><section className="grid gap-6 xl:grid-cols-[1.35fr_0.65fr]"><Card className="p-0"><div className="p-6"><h3 className="text-xl font-black">User Management</h3><p className="mt-1 text-sm text-white/45">Review creator accounts and performance.</p></div><Table columns={["Creator","Status","Revenue","Conversions","Action"]} rows={adminUsers} renderRow={(user) => <tr key={user.handle} className="text-white/65 transition hover:bg-white/[0.03]"><td className="px-5 py-4"><p className="font-semibold text-white">{user.name}</p><p className="text-xs text-white/38">{user.handle}</p></td><td className="px-5 py-4"><span className="rounded-full bg-white/[0.05] px-3 py-1 text-xs">{user.status}</span></td><td className="px-5 py-4">{user.revenue}</td><td className="px-5 py-4">{user.conversions}</td><td className="px-5 py-4"><button className="font-semibold text-ember-500 hover:text-white">Review</button></td></tr>} /></Card><Card><h3 className="text-xl font-black">Payout Approvals</h3><div className="mt-6 space-y-4">{payouts.map((payout) => <div key={`${payout.creator}-${payout.date}`} className="rounded-2xl border border-white/[0.06] bg-black/25 p-4"><div className="flex items-center justify-between gap-4"><div><p className="font-bold">{payout.creator}</p><p className="text-sm text-white/42">{payout.date}</p></div><p className="font-black">{payout.amount}</p></div><Button variant={payout.status === "Pending" ? "primary" : "secondary"} className="mt-4 w-full py-2">{payout.status === "Pending" ? "Approve" : "Approved"}</Button></div>)}</div></Card></section><section className="mt-6"><Card><h3 className="text-xl font-black">Campaign Monitoring</h3><div className="mt-6 grid gap-4 md:grid-cols-3">{campaigns.map((campaign) => <div key={campaign.name} className="rounded-2xl border border-white/[0.06] bg-black/25 p-5"><p className="font-bold">{campaign.name}</p><div className="mt-5 flex justify-between text-sm text-white/55"><span>Spend</span><span>{campaign.spend}</span></div><div className="mt-2 flex justify-between text-sm text-white/55"><span>ROI</span><span>{campaign.roi}</span></div><div className="mt-4 h-1.5 rounded-full bg-white/10"><div className="h-full w-3/4 rounded-full bg-ember-500 shadow-glow" /></div><p className="mt-3 text-xs font-semibold text-ember-500">{campaign.health}</p></div>)}</div></Card></section></DashboardShell>;
}
